select * from booking_details;

desc booking_details;

desc user_profile;
desc CUST_CREDENTIALS;
desc customer;

CREATE TABLE CUST_CREDENTIALS (
    CUST_ID INT NOT NULL,
    CUST_NAME varchar(255) ,
    MOBILE varchar(255) ,
	EMAIL varchar(255) ,
    OTP VARCHAR2(50),
    password VARCHAR2(255),
    CONSTRAINT CUST_CREDENTIALS_PK PRIMARY KEY (CUST_ID,MOBILE)
);
commit;
select * from CUST_CREDENTIALS;
insert into CUST_CREDENTIALS(CUST_ID, CUST_NAME, MOBILE, EMAIL, OTP) values(1001,'ravikiran','9164701008','mravikiran495@gmail.com',4598);

desc CUST_CREDENTIALS;

ALTER TABLE CUST_CREDENTIALS
MODIFY OTP VARCHAR2(30);

ALTER TABLE CUST_CREDENTIALS MODIFY MOBILE datatype NULL;


CREATE TABLE CUSTOMER_DETAILS (
    CUST_ID INT NOT NULL,
    C_FIRSTNAME varchar(255) ,
    C_LASTNAME varchar(255) ,
	C_MOBILE varchar(255) NOT NULL,
    C_EMAIL VARCHAR(225) ,
    CONSTRAINT CUSTOMER_DETAILS_PK PRIMARY KEY (CUST_ID,C_MOBILE)
);
commit;
INSERT into customer_details(cust_id, c_firstname, c_lastname, c_mobile, c_email) values(1001, 'ravikiran','mukiri','9164701008','mravikiran495@gmail.com');
desc address;

drop table CUSTOMER_DETAILS;

CREATE TABLE ADDRESS (
    ADDRESS_ID INT NOT NULL PRIMARY KEY,
    CITY VARCHAR(50) ,
	STATE VARCHAR(50) ,
	ZIPCODE VARCHAR(50) ,
	MOBILE VARCHAR(50) NOT NULL,
    CUST_ID INT ,
    CONSTRAINT ADDRESS_PK FOREIGN KEY (CUST_ID)
    REFERENCES CUSTOMER_DETAILS(CUST_ID)
);

drop table address;

ALTER TABLE CUST_CREDENTIALS
ADD password VARCHAR2(255);

select * from CUST_CREDENTIALS;

CREATE TABLE VENDOR_ADDRESS (
    V_ADDRESS_ID INT NOT NULL PRIMARY KEY,
    V_ADDRESS1 VARCHAR(50),
    V_CITY VARCHAR(50) ,
	V_STATE VARCHAR(50) ,
	V_ZIPCODE VARCHAR(50) ,
    VENDOR_ID INT ,
    CONSTRAINT VENODR_ADDRESS_PK FOREIGN KEY (VENDOR_ID)
    REFERENCES VENDOR_DETAILS(VENDOR_ID)
);
drop table vendor_details;
commit;
CREATE TABLE VENDOR_DETAILS (
    VENDOR_ID INT NOT NULL,
    V_FIRSTNAME varchar(255) ,
    V_LASTNAME varchar(255) ,
	V_MOBILE varchar(255) NOT NULL,
    V_EMAIL VARCHAR(225) ,
    CONSTRAINT VEND_DETAILS_PK PRIMARY KEY (VENDOR_ID)
);

CREATE TABLE MYBOOKINGS (
    BOOKING_ID INT NOT NULL PRIMARY KEY,
    BOOKING_STATUS VARCHAR(50),
    CUST_ID INT NOT NULL,
    VENDOR_ID INT NOT NULL,
    FOREIGN KEY (CUST_ID) REFERENCES CUSTOMER_DETAILS.CUSTOMER_DETAILS(CUST_ID),
    FOREIGN KEY (VENDOR_ID) REFERENCES VENDOR_DETAILS.VENDOR_DETAILS(VENDOR_ID)
);


sqlplus / as sysdba

SELECT name, open_mode FROM v$pdbs;

ALTER PLUGGABLE DATABASE ALL OPEN;
-- or
ALTER PLUGGABLE DATABASE PDB1 OPEN;

GRANT REFERENCES ON CUSTOMER_LOGIN.CUSTOMER_DETAILS TO MYBOOKINGS;
GRANT REFERENCES ON VENDOR_LOGIN.VENDOR_DETAILS TO MYBOOKINGS;

ALTER SESSION SET CONTAINER = orclpdb;

CREATE USER MYBOOKINGS IDENTIFIED BY password;
GRANT CONNECT, RESOURCE TO MYBOOKINGS;
ALTER USER MYBOOKINGS QUOTA UNLIMITED ON USERS;

1. Connect using SQL Developer
Use these settings:

Username: vendor_login

Password: StrongPassword123

Hostname: localhost

Port: 1521

Service name: orclpdb

CREATE SEQUENCE CUST_CREDENTIALS_SEQ
  START WITH 1
  INCREMENT BY 1
  NOCACHE
  NOCYCLE;